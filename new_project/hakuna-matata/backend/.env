PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
OPENAI_API_KEY=
# No MONGO_URI -- database removed. This backend is minimal and the app uses
# client-side storage (localStorage) for user and session data.
